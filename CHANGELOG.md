# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范，版本号以 Git Tag 形式发布（`V主版本.次版本.修订号`）。

## [V1.1.0] - 2026-09-11

事件能力的大版本更新：补齐时间表达、重复事件、检索与时间轴视图。

### 新增

- **事件备注** — 事件支持多行备注（`description`），详情弹窗与日视图展示
- **时间范围** — 事件支持开始/结束时间（`end_time`），表单使用时间范围选择器；校验「结束时间不能早于开始时间」
- **全天事件** — 新增 `all_day`，开启后自动清空时间，日视图置顶展示
- **重复事件**
  - 支持「每天 / 每周 / 每月 / 每年」重复，可指定重复结束日期
  - 采用实例化存储，通过 `recurrence_group` 关联同一系列
  - 每月重复自动处理月末（`1/31 → 2/28 → 3/31 → 4/30`，不产生日期漂移）
  - 编辑/删除支持「仅此事件 / 整个系列」两种范围
  - 未指定结束日期时默认重复 90 天，单次最多生成 400 个实例
  - 视图上以 🔄 图标标识重复事件
- **搜索 / 筛选** — 新增 `GET /api/events/search`，支持关键字（标题或备注模糊匹配）、日期范围、颜色、完成状态组合查询
- **搜索弹窗** — 前端新增 `SearchModal`，结果可一键跳转到对应日期并打开详情，也可直接勾选完成
- **拖拽调整事件**
  - 月视图：拖拽事件到其它日期
  - 周视图：拖拽事件到其它日期；拖动时显示整点时间槽，拖入可同时修改时间
  - 修改时间时按原时长自动平移结束时间（如 `10:00-11:00 → 14:30-15:30`）
- **日视图时间刻度轴** — 日视图改为 24 小时刻度轴布局：事件按开始时间定位，方块高度对应时间范围；全天或未设置时间的事件置顶显示；含当前时间红线、自动滚动到首个事件；时间重叠时以泳道并排兜底

### 改进

- **月/周视图事件条不再显示时间**，仅保留标题与重复标记，版面更清爽
- README 功能概览与项目结构说明同步更新

### 数据库变更

- **Schema 版本：v1.3 → v1.4**
- `events` 表新增列：`end_time`、`all_day`、`description`、`recurrence`、`recurrence_end`、`recurrence_group`
- 增量迁移：SQLite 使用 `ALTER TABLE ADD COLUMN`，MySQL 先查 `information_schema` 再 `ALTER TABLE`；**旧库自动升级，无需删库**
- 修复 v1.3 时区迁移缺少版本门控的问题：此前每次 schema 版本变更都会重复执行 `+8 小时` 迁移；现在仅在从 v1.3 之前的版本升级时执行一次

### 升级说明

- 直接更新代码并重启服务即可，数据库会在启动时自动完成 v1.4 迁移
- 前端需重新构建产物（`npm run build`）后再部署；Docker 部署重新构建镜像即可
- 向后兼容：旧事件数据不受影响，新字段默认为空

---

## [V1.0.3] - 2026-09-11

### 改进

- 周视图事件标题完整显示（自动换行，不再因超长被截断）
- 日视图事件配色统一

## [V1.0.2] - 2026-09-10

### 改进

- 日视图事件颜色条自适应事件高度

## [V1.0.1] - 2026-09-08

### 修复

- 修复事件详情弹窗中「标记完成 / 取消完成」提示重复弹出的问题

## [V1.0.0] - 2026-09-05

首个发布版本。

### 功能

- 月 / 周 / 日三视图，农历、二十四节气、法定节假日与调休标注
- 事件管理：创建、编辑、删除、标记完成、颜色标记
- Excel 导入导出（保留颜色与完成状态）、导入模板下载、重复数据自动跳过
- 多用户隔离与权限管理，管理员面板、审计日志、登录日志
- 登录安全：bcrypt 密码哈希、失败锁定、密码复杂度校验
- AI 总结：周 / 月 / 季度 / 年度报告，支持自定义提示词、无 AI 时自动降级
- Docker + Docker Compose + Nginx + Gunicorn 部署，SQLite / MySQL 一键切换
